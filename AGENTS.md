# outro —— 在这里干活的规矩

结尾页生成器：填几个字段，产出一张可以直接截图的结尾页。给 AI 看的东西只有这一份，改规矩就改这里。

这份文件跟着现实走：改动只要动了规矩、命令、目录结构或环境事实，就在同一次提交里一起改。它跟代码对不上，比没有它更糟。

## 怎么干活

- 动手前先用中文把计划一次说清，不拆成一串小确认
- 回答简短：能上表格就上表格，只给结论、改动文件、验证结果，不复述过程
- 工具调用能并行就并成一批
- 省 token：对账性质的测试、探针、线上核对一律不跑；能读代码判断就读代码；命令能合并就合并
- 单条命令不超过 10 秒，跑不完就拆开，或直接说这条跑不动
- 改完把两条闸门合成一条跑（`npm run typecheck && npm run build`），过了就提交；已读过的文件不重读，不顺手再量别的
- 设计取舍拿不准先问，不替人拍板；被否掉的方向不要换个说法再来

## 提交

- 每做完一件事提交一次
- 信息一行：`<type>: <description>`，英文、祈使句、≤50 字符、不带标点；type 取 feat / fix / docs / style / refactor / test / chore
- 不 push，除非明确要求；要 push 时用 `GIT_SSH_COMMAND="ssh -F /dev/null" git push`。push 后不验线上，发布是自动的
- `dist/` 不入库，页面由 GitHub Actions 构建；`vite build` 会清空 `dist/`，临时工具放 `.git/` 下

## 验证

没有 lint。闸门就是这两条，改完合成一条跑：

```bash
npm run typecheck   # tsc --noEmit
npm run build       # vite build，产出单文件 dist/index.html
```

- dev server 在 http://localhost:5173，常年有一个后台任务，不要另起；改完文件 Vite 自动热更新
- Vite 的开发态转换按秒缓存：一批里改完同一个文件后 `touch` 一下，再 `curl` 比特征串，确认喂的是新版。判断模块是否更新要逐个 `curl`，只查一个不下结论，也别让人刷新去猜
- 样式在浏览器里看不出变化时，先重启 dev server 再查代码：旧进程会一直喂改前的样式，硬刷新无效；重启也能清掉积坏的 HMR 状态
- 判断代码对不对不靠浏览器现象：`curl` `src/style.css?direct` 看编译出的规则；要真实几何用 headless Firefox 的 BiDi
- 探针是慢路径，只在读代码判断不了、改错代价又大时用：脚本在 `.git/` 下（不入库），要新量什么就给已有的加一行，一个脚本里把宽度与路由循环量完，用完 `job_kill` 掉 Firefox。纯样式或文案改动不量
- 两个探针事实：Firefox 的 profile 目录要先建，只允许一个 BiDi 会话，端口被占就换 profile 与 `PROBE_PORT`；`(hover: hover)` 在 headless 里为 false，自己的悬停样式量不到，但悬停状态可用 `matches(':hover')` 量。媒体查询宽度把滚动条算进去
- 沙箱里杀不掉别人终端的 dev server，要重启得请用户来；自己起要用后台任务（普通 `nohup` 会随调用被回收），后台任务跨轮存活，换掉得 `job_kill` 再起
- 杀进程不用 `pkill -f`（会匹配到自己那条命令），用 `pkill -x` 或先存 PID
- 比字节用 `wc -c`：`$(...)` 会吃掉末尾换行，`.length` 是字符数
- 纯注释的活以「剥掉注释后与 HEAD 逐行相同」自证零代码改动，按文件逐个比；注释行数不是判据
- 装依赖用 `npm ci --cache /tmp/npm-cache`

## 代码约定

- 一维排布一律用 flex（行、列、一维的组合、两栏分比例）：`flex` 加 `gap-*`。每条 flex 列写 `min-w-0`，否则内容按固有宽度把行顶出屏幕；`items-start` 只写在行方向。给条件渲染的块留间距用父层 `gap`，不用 `space-y-*` 或 `mt-*`。`grid` 只给真二维且行列都要对齐的地方（现仅元数据表），必须显式写列模板
- `<label>` 只包一个控件，不包一组选项：它只认第一个可标记后代，包住一排选项时整块区域的悬停与点击都算在第一颗上。一组选项用 `<div role="group" aria-label>`
- 界面上的符号一律用图标组件（`ui/icons.tsx` 包 `lucide-preact`），不写字体字符：字形随系统字体变。图标尺寸与颜色写死在 `icons.tsx` 里，调用点不管；分隔线、下划线用元素画
- 依赖按需装。看包的形状：`sideEffects: false` 加一个功能一个模块的摇得干净，核心本身是运行时的摇不掉。别为省体积自己抄精简版
- 一个知识只有一个定义，注释也一样：跨文件成立的推论写一处，别处只引用。文案在 `lib/copy.ts`，内容由 `lib/outro.ts` 的 `resolveOutro` 决定，框的外观在 `ui/inputs.tsx`，排版与动效片段在 `ui/tokens.ts`，可点的整行是 `ui/LinkList.tsx`
- 目录表叫 `_registry.ts(x)`。`src/surveys/` 是首页入口（一份一个文件加一行），`src/steps/` 是向导三步
- 存图：把屏幕上那一份原样装进 SVG，让浏览器画到画布上，一档一颗按钮，没有第二套渲染路径（`lib/image.ts`）
  - 克隆页面外壳并带上应用的 CSS，装进一个 SVG 视口；视口就是 `data-card` 那一块。排版在设计宽下解一遍：外壳按设计宽摆好，再往左挪掉卡片左边空出的那段（由居中算得），卡片正好落进视口，图上不裁
  - 三档不在画布上做文章，而在排版宽上：每档写死一个设计宽（456 / 640 / 1148）与比例（2:3 / 1:1 / 3:2），出图前把结尾页挂进屏幕外的取景台按那个宽排一遍
  - 设计宽只定档，不定卡片的宽：卡片在台子里照旧按页面那套规矩定宽。给卡片钉 `min-width` 会盖掉它的宽度上限
  - `data-card` 量到的宽度要钉回克隆（图里没有滚动条），高度由卡片定；克隆里摘掉「至少一屏高」
  - 画布尺寸取卡片自己的宽高，四周各留较长边的四分之一（不少于），再按比例补齐成那一档；边长超过上限就整体等比缩；底色取外壳底色；渲染倍率写死 2，不跟设备像素比
  - 不用 `html-to-image` 那类「读算完的样式冻进克隆」的做法，结果会漂
  - 最终页里没有控件，返回靠点任意处，动作全在向导第三步。控件与卡片分开：控件进卡片会改变署名折行
- 问卷是数据不是代码：题目写 `into` 决定答案落到结尾页哪里（`meta` / `block` / `title` / `footer`），需要加工才写 `build`；每道题先写 `into`。加一份问卷不碰组件
- 问卷答完以 `DEFAULT_CARD` 为底，把答到的部分盖上去，不是清空：`buildCard` / `Survey.build` 返回 `Partial<CardData>`，只含问到的去处；问到却答空的标题与页脚写成空串，元数据与文本块的空答案丢掉
- 回到表单：内容已成型就停在最后一步（`formAtLastStep`，用于结尾页点任意处与答完问卷）；没有「刚才那一步」才从第一步进（`openForm`，首页的编辑表单）
- 按钮四种变体只差颜色，几何一致：外高 40px、横向内边距 32px、描边一律 1px（有描边的那两种从内边距里扣掉）。危险动作靠颜色与文案，不靠加粗或放大
- 页脚那种链接是导航清单不是按钮（`ui/LinkList.tsx`）：`<nav>` 里 `<ul>`，每项一个 `<a>`；竖向紧挨、行间无间隙，平时淡一档、悬停转 blue 加下划线，只变文字色不加底色
- 同一标题层级必须同款：页面标题与页脚两栏标题共用 `tokens.ts` 的 `HEADING`；印在卡片里的标题是另一款，各只有一处所以写在组件里
- 注释写中文，只写设计想法，不写实现、过程、数值与类名；点组件 / 文件 / 函数名可以。prop 的一行说明与「为什么写死」属于定义，不要砍。迁移锚点（认旧档用的旧键等）要留，并注明不代表今天
- 不写自定义 CSS。例外只有 `style.css` 里的 `.safe-area`（要读 `env()`）与 `@custom-variant press`
- Tailwind 的扫描来源写死在 `style.css` 头上（`source(none)` 加两条 `@source`）：默认它扫整个仓库，把散文里的词编译成规则。加新目录要补 `@source`；扫描器不解析语法，注释与字符串里的类名一样进产物

## 响应式

- 三个模式，只看宽度（数字在 `style.css` 的 `@theme`，判断交给 Tailwind，代码里不出现宽度数字，也不用 `matchMedia` / `ResizeObserver`）：窄 < 30rem（480px，`max-narrow:`）、中 30rem–64rem（默认样子）、大 ≥ 64rem（`wide:`，分栏加右侧清单常驻）
- `landscape:` / `portrait:` 已退役，不再使用
- 窄与宽量的是框不是窗口（编译成容器查询）：页面里框等于页面宽，出图时框等于该档设计宽，同一张成品在任何窗口上都排成同一个样子。量具只有 `PageShell` 的外壳一处，别再加第二层容器（查询认最近的一层）。判据写成 `(width >= 数字)` / `(width < 数字)`
- 窄屏是一维的流：容器不给横向留白，面横向贴边并去掉侧边描边与圆角，横向留白由文字与控件自己带一次（全站唯一那个 16）。裸控件在窄屏不带横向内边距；贴边的面里的裸文字与裸列表要自己补上
- 元数据行窄屏一律上下排（编辑态与清单同一条规矩）：名称与值算一条，对内紧于条间。最终页不在此列，它那张表直接躺平
- 换行要么由档位定，要么由内容定：结构按档位写死（页脚两端、动作行、两栏），`flex-wrap` 只留给条数与长度由数据决定的排
- `grid` 一定显式写列模板，不写就是隐式 `auto`，按内容算且不收缩
- `<button>` 宽度按内容收缩，整行可点就用 `<a>`；它在 flex 列里会被拉满整行，要内容宽就写 `self-start`
- 动效只有渐变：`tokens.ts` 的几份都长在同一份清单上（透明度、文字色、底色、描边色、显示），组件里不散写；150ms、ease-out、不回弹，`motion-reduce` 下不动。几何量一概不插值（内边距、描边、圆角、位移、缩放）；跨档不做任何动画，连整体淡一下都不要
- 可点件的反馈写 `press:`，展开成带媒体查询的悬停加裸按下；只写 `hover:` 在触摸设备上零反馈。反馈是元素自己变色，不位移不换形状；框里的图标按钮连淡底都不给
- 最终页（`OutroPage`）也跟这三档，它是拿去截图的那一屏：
  - 要一致的是栏（一行）的宽，不是容器的宽：容器上限按栏宽反推。窄中档一行 24rem、左右各 16；宽档两栏各 18rem、间距 3rem、左右各 16，按 1:1 分。内边距只有一个数（16）
  - 整块横竖居中，内容比屏幕高时退回顶部排
  - 三段（标题 / 主体 / 页脚）在同一个 flex 列里，段间距一律 12，页脚不单独挂外层。它身上没有面，窄屏内化不用做任何事
  - 不按档位改结构：页脚与元数据行只有一种排法，没有最窄模式
- 三档成品：竖版 2:3、设计宽 456，一行 416 的单栏；方版 1:1、640，同一单栏版面，只是画布比例不同；横版 3:2、1148，一行 656 的分栏（两栏各 288）。四周留白每边不低于排版区较长边的四分之一

## 内容与文案

- 类别名写全，不缩写
- 选项表是常用值不是全集：长一点的单选要留「自定义」的口子，自己写的一句也能进结尾页。多行题也能带选项：预设是一整段，竖排成整宽的一块块，点一条整段填进框；行为与单选同一套（互斥、点「自定义」才出框、× 退回预设、再点一下取消、点是覆盖不是追加）
- × 要看起来嵌在框里：靠组合拿到，不塞进 `<input>`
- 没有兜底文案，留空就是不印：标题空着，标题块连横线整个不渲染；页脚空着，署名那行不渲染。给条件渲染的东西留间距用 `gap`，不用会被孤立下来的边距。占位提示不是数据，全站只有一句「不显示」
- 预填值是真值，会一路印到结尾页；「默认不显示这一行」用不写预填值表达。问卷数据里没有只管控件长相的字段，多行框固定三行、不开成 prop
- 空答案整条丢掉，不在结尾页留空标签
- 清单哪一段空着，就在那一组里放一个虚线提示，提示只说这一段是空的。提示只带框内部的内边距，所以清单的窄屏内边距写在有内容的那几个分支上
- 不用截断：名称与值写长了就换行
- 文案不为某一档定制，不写方位、不暗示结构：「右侧清单」「右上角」「第 N 步」都会在别档失效；要指位置只用不随档位变的粗说法，拿不准就不提

## 环境

- 技术栈：Vite + Preact + TypeScript（strict）+ Tailwind v4 + `vite-plugin-singlefile`；图标用 `lucide-preact`（按需）。存图不装库
- 仓库 `omninbs/outro`（公开），线上 https://omninbs.github.io/outro/
- 数据存在浏览器 localStorage（`outro.card.v2`，旧键 `colophon.card.*` 会迁移），不经过后端
- 发布：`.github/workflows/deploy.yml`，push 到 main 自动跑。`concurrency: pages` 且 `cancel-in-progress: false`，卡住的 run 会挡住后面的，要去 Actions 取消
- 发给别人：`cp dist/index.html dist/outro.html`，自包含，`file://` 直接打开，数据只存在对方浏览器里
- `index.html` 的 `viewport-fit=cover` 与 `style.css` 的 `.safe-area` 是一对：`.safe-area` 只挂最外层根节点；删掉那个 meta，这段安全区留白会静默失效
