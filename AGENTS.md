# outro —— 在这里干活的规矩

结尾页生成器：填几个字段，产出一张可以直接截图的结尾页。给 AI 看的东西都写这一份，改规矩就改这里。

**这份文件跟着现实走**：任何改动只要动了规矩、命令、目录结构或环境事实，就在同一次提交里把它一起改掉。它跟代码对不上，比没有它更糟。

## 怎么干活

- 动手前先用中文把计划说清，一次说完，别拆成一串小确认
- 回答简短：能上表格就上表格；不复述过程，说清结论、改动文件、验证结果就够
- 工具调用能并行的并成一批发出去
- **单条命令不超过 10 秒**。跑不完就拆开跑，或者直接说「这条跑不动」，不要硬试
- **以快为先：验证能省就省**。改完把两条闸门合成**一条**命令跑（`npm run typecheck && npm run build`），过了就提交；已经读过的文件不重读，几条能合的命令合成一条（`&&`），别顺手再去量点别的。探针、起浏览器、翻线上这些一律是**慢路径**，默认不走，理由见「验证」那一节
- 设计取舍拿不准先问，不替人拍板；被否掉的方向不要换个说法再端上来

## 提交

- 每做完一件事提交一次，不攒着
- 信息只有一行：`<type>: <description>`——英文、祈使句、≤50 字符、不带标点
- `type` 取 `feat` / `fix` / `docs` / `style` / `refactor` / `test` / `chore`
- **不 push**，除非明确要求。真要 push 时用 `GIT_SSH_COMMAND="ssh -F /dev/null" git push`（本机 ssh 配置有一处权限问题）。push 完**不要**再去验线上（`curl` 比字节、翻 Actions 都算白花 token）：发布是自动的，要确认也是用户自己看
- 构建产物不入库：`dist/` 在 `.gitignore` 里；页面由 GitHub Actions 构建，push 到 main 自动发布
- `vite build` 会清空 `dist/`：临时工具别放那儿，放 `.git/` 下（git status 看不见，build 也清不掉）

## 验证

没有 lint，这两个命令就是全部闸门，改完必跑：

```bash
npm run typecheck   # tsc --noEmit
npm run build       # vite build，产出单文件 dist/index.html
```

- dev server 在 http://localhost:5173（`npm run dev`，常年有一个后台任务跑着，**不要另起一个**）；改完文件 Vite 自己热更新
- 注意 Vite 的开发态转换按秒缓存：同一秒里连改同一个文件两次，可能喂出半新半旧的模块，`touch` 一下强制重转。这条真栽过：一次批量改完之后 dev server 一直喂「import 已删、`${MORPH}` 还在」的半成品，浏览器报 `ReferenceError: MORPH is not defined`。判断办法是 `curl -s http://localhost:5173/src/…` 直接看它喂的是什么，`touch` 掉那几个文件再 curl 一遍确认——**别让人去刷新猜**。2026-09 又栽一次，这次认得清楚了：一条消息里对 `app.tsx` 连做三处编辑（重命名 `backToForm` → `formAtLastStep`、改它的两处调用点），dev server 喂出来的模块里**新旧名字同时存在**（`formAtLastStep` 1 处、`backToForm` 1 处、`setStep(0)` 还有 3 处＝改之前那版），用户看到的就是「改了没生效」。所以规矩加一条：**同一个文件在一批里改完之后，收尾 `touch` 一下、再 `curl` 比一次特征串**（`touch` 会触发重转并推 HMR），确认喂的是新版再去说「刷新看看」——省下的是两个人一轮的来回
- 改完样式在浏览器里看不出变化时，**先重启 dev server，再查代码**：旧进程会把改之前编译好的样式一直喂给新开的标签页，硬刷新、换标签都没用（2026-09 那次「窄屏断点没生效」就是这么白查了一轮）。重启还能清掉积坏的 HMR 状态——同一个月里遇到过一次 `#app` 渲染成空、typecheck/build 却全过，重启就好了
- **半新半旧比整体旧更难认**：同一个 dev server 里 `copy.ts`、`outro.ts` 都已经是新的，偏偏 `OutroPage.tsx` 停在改动前（`curl` 出来 `jsxDEV(OutroHeader, …)` 前面没有 `title &&` 那层判空、`lineNumber` 还指着旧的一行），于是「全部留空」时页面照样画出标题块和那根横线，看着像改了没用。判断只能**逐个模块 `curl` 比**（拿源码里的特征串去对），别只 curl 一个文件就下结论，也别让人刷新猜
- 反过来，判断「代码对不对」不要靠浏览器里的现象：`curl` dev server 的 `src/style.css?direct` 看编译出来的媒体查询；要量真实几何就用 headless Firefox 的 BiDi 口
- **探针是慢路径，慎用**（2026-09 用户明确要求「尽可能快」）：起 Firefox、为了释放会话再重启、一个脚本量十档宽度，一轮就是好几分钟。只在**读代码判断不了、改错了代价又大**的时候才用（真实几何 / 断点 / 真实点击这类），而且要：用 `.git/` 里**已有**的那个脚本（需要新量什么就给它加一行，别为一次改动新写一个脚本）、一个脚本里把宽度 / 路由循环量完、量完立刻把 Firefox 那个后台任务 `job_kill` 掉。纯样式或纯文案的小改动，读一遍代码 + 跑一次闸门就提交
- **探针**（`.git/` 下，不入库）：`mkdir -p .git/ffprof && firefox --headless --no-remote --profile "$PWD/.git/ffprof" --remote-debugging-port=9222 about:blank`——profile 目录**必须先存在**，不然 Firefox 直接退出报「Could not find profile folder」；放 `.git/` 是因为 `vite build` 会清掉 `dist/`。然后 `node .git/probe-overflow.mjs 485 481`（找横向溢出的元素）、`probe-motion.mjs`（跨断点采样 padding，几何量应当是直接跳的）或 `probe-outro.mjs`（最终页版面：量宽度上限 / 居中 / 分栏条件 / 有没有溢出，自带一份种进 localStorage 的存档，种完要**重新加载**才生效）、`probe-back.mjs`（点结尾页的「返回编辑」，看落在第几步）、`probe-empty.mjs`（标题 / 页脚留空时结尾页与清单各该消失什么，以及**答完问卷后默认值还在不在**——它会点一次问卷页的「完成」再读存档；注意只换 hash 是同文档导航，应用不会重读 localStorage，得真重新加载）、`probe-headings.mjs`（各档下把页面里每个 h1/h2/h3 的字号 / 行高 / 字重 / 字距 / 颜色 / 下边距列出来，用来比「同层标题是不是同款」——比肉眼看截图靠谱）、`probe-link.mjs`（页脚那两条链接：行宽是不是整栏、平时和悬停各是什么颜色 / 有没有下划线 / 底色透不透明，顺带把参考站 book.kemya.net 的页脚一起量了对照；末尾还会真点一下「返回主页」，看 hash 与页面有没有回到首页）、`probe-hover.mjs`（悬停**状态**：把指针放到指定的一串位置上，逐个报「指针下是谁」和「哪些元素真的 `matches(':hover')`」——2026-09 就是靠它定出「`<label>` 包住一排选项时，指针落到栏里的空白或第二颗上，第一颗照样算悬停」这个 bug 的）、`probe-image.mjs`（窄中宽三档各在结尾页点一次「保存为图片」：① 对账——PNG 是不是方形、边长是不是卡片最长边的两倍；② **对像素**——把屏幕截图里卡片那一块与成品图里那一块逐像素比，两边该是同一份东西，只该在字形的栅格化上有差。2026-09 这一条量出过真问题：旧做法下屏幕墨迹 16440 / 成品 15876（字号被压了 0.1px），墨迹框也差 2px；换掉之后两边墨迹框完全相同、差只剩 0.7–1%）——探针默认打 `dist/index.html`，免得吃 dev server 的旧模块；要故意打 dev server 就加 `PROBE_ORIGIN=http://localhost:5173/`（这时 localStorage 也是那个来源的，跟 `file://` 那份不互通）。Firefox 只允许**一个** BiDi 会话，所以一个脚本里把要量的宽度 / 路由循环完，别一个宽度起一次（一个脚本跑完就 `ws.close()`，否则下一次会报「Maximum number of active sessions」，那就得重启 Firefox）。**该数个数的地方要数一遍**，别只量位置：只有一份的东西（`h1`、`footer`）漏出第二份时，位置照样是对的——2026-09 最终页上就并排渲染过两个页脚（一次编辑补上了新行、忘了删旧行，typecheck 与 build 都看不出来，机器看页面也只觉得「页脚矮一点」），所以 `probe-gap.mjs` 顺手报 `h1` / `footer` 的个数
- 量响应式时记住：**Firefox 的媒体查询宽度把滚动条算进去**，排版区不算（窗口 485 → 媒体查询按 485 判，`clientWidth` 只有 473）
- **headless Firefox 里 `(hover: hover)` 是 false**（`pointer: fine` 也是），而 Tailwind 的 `hover:` 一律编译进 `@media (hover: hover)`——所以**探针量不到我们自己的悬停效果**：`probe-link.mjs` 里悬停前后一模一样不等于没生效（参考站那种裸 `:hover` 才量得到，一量就有）。要验悬停就查编译产物里那条规则在不在，比如 `grep -o '[^;{}]*text-underline-offset:[^;}]*' dist/index.html`，别把「探针没变化」当成 bug 去改代码。**但悬停的「状态」量得到**：`matches(':hover')` 不看媒体查询，`probe-hover.mjs` 就是靠它把「指针落在选项栏的空白处 / 第二颗按钮上时，第一颗也算悬停」定死的——样式量不到不等于什么都量不到
- 沙箱里 `ss` 看得到端口、却看不到别人的 PID：**杀不掉你终端里那个 dev server**，要重启得请你来。自己起也留不住——`setsid nohup npm run dev &` 那条命令一返回进程就没了（harness 按调用回收整棵进程树，脱会话也躲不掉），要用只能用后台任务起，而**后台任务那份跨轮还活着**（2026-09 实测：本轮开头起的 `npm run dev` 十几轮之后仍在 `job_list` 里 `running`，dev server 也一直响应），所以它不会自己死——想换掉得 `job_kill` 再起；但它是**这个会话的**，会话结束就没了，「常年开着」那一份仍旧得由用户在自己终端跑
- 杀进程别用 `pkill -f '关键词'`——模式会匹配到自己那条命令行，整条命令被杀（退出码 143）。用 `firefox … & ffpid=$!` 存 PID，或 `pkill -x 名字`
- 比字节用 `wc -c`：`$(...)` 会吃掉末尾换行（差 1 字节），JS 的 `.length` 是字符数不是字节数
- **纯注释的活用「剥掉注释后与 `HEAD` 逐行相同」自证零代码改动**：`git show HEAD:<file>` 与工作区各自删掉 `//` 与 `/* */` 之后逐行比对，比人眼读 diff 可靠（2026-09 全仓精简注释，三十来个文件都靠它验收，连带确认没顺手改坏一行）。这类活值得跑，平时改代码时看 diff 就够了。判据是「剥掉注释后逐行相同」，**别拿注释行数当判据**：`grep -cE '^[[:space:]]*(//|\*|/\*)'` 数不到 JSX 里的注释块与不带 `*` 的续行，会给出「数字一点没降」的假象；多人同时改注释时 `git diff` 还会混进别人正在改的文件，得**按文件**逐个比
- 装依赖用 `npm ci --cache /tmp/npm-cache`（沙箱里默认缓存目录只读）
- 产物是自包含的单个 HTML：`dist/outro.html` 发给别人、`file://` 直接打开就能用（**不用**在 push 后去比对线上那一版）

## 代码约定

- **一维的排布用 flex**（**用户明确偏爱**，2026-09 反复要求过：flex 更现代，一维的组合也照用）：一行或一列都写成 `flex` + `gap-N`（`flex-row` / `flex-col`），**一维的组合**（列里嵌行、行里嵌列）也一样，别因为嵌套就换工具；**两栏分比例也归它**（向导 `wide:flex-[1]` / `wide:flex-[1.1]`、页脚 `wide:flex-[7]` / `wide:flex-[5]` 都是这么写的）。两条容易忘的：① **每一列都要写 `min-w-0`**——flex 项默认 `min-width: auto`，会按内容撑开、不收缩，裸输入框那 400px 出头的固有宽度能把整行顶出屏幕（原来 grid 那边是 `minmax(0,1fr)` 在管这件事）；② **`items-start` 只写在 `flex-row` 那一档**——在列方向上它是横向对齐，会把子项缩成内容宽。`space-y-*` 2026-09 全清掉了：它靠 `> :not(:last-child)` 的边距撑开，块不在了那份距离还在（跟 `mt-12` 一个毛病），子元素自己的边距也会跟它合并。`grid` 只留给**真二维、行列都要对齐**的地方（现在只有元数据那张 `dl`：名称列要跨行对齐），而且必须显式写列模板
- **`<label>` 只包「一个控件」，别包「一组选项」**（`ui/Field.tsx` 的 `group` 开关）：`<label>` 的「可标记后代」只认**第一个**，包住一整排选项时，整块区域（题面、栏里没按钮的空白、按钮之间的缝）的悬停与点击都算在第一颗头上——2026-09 用户报「悬停在第二三颗上，第一颗也响应」，`probe-hover.mjs` 量到的原样是：指针在栏右端空白 / 一二颗的缝 / 题面上时，第一颗照样 `matches(':hover')`；指针在第二颗上时两颗都算。而且点栏里的空白会直接**选中**第一颗。那本来就不是「一个控件」，所以摆着一组选项时只当 `<div role="group" aria-label>`；`<label>` 留给「里面就一个框」的情况（点题面聚焦进框）
- **界面上的符号一律用图标组件，不写字体字符**（`ui/icons.tsx` 包着 `lucide-preact`）：`×` `＋` `✓` 原来是字体字符，字形由**系统字体**决定——同一个勾，换台机器宽窄、粗细、基线都不一样；**步骤条圆里那三个数字同理**（用户完全可能把系统默认字体设成拼音字体 / 艺术字），所以那里也放图形：**语义图标**（`list` / `pen-line` / `sparkles`，写在 `STEPS` 每条自己的 `icon` 上），「第几步」靠位置读、完成态仍旧是那颗勾。`tally-1/2/3` 是**想过但不成立**的一条：它是一组**左对齐的刻痕**（`tally-1` 的墨迹在 24 框的 `x=4`，三颗的组中心分别是 4 / 6.5 / 9，`tally-5` 才铺满整框），塞进「一步一颗居中圆」要么偏左且只有 7px 高，要么裁 `viewBox` 让笔画变成 2.3px——比旁边那颗勾（1.2px）粗一倍，一排里出现两种笔画重量；`＋` 更明显，它是全角字符，连占多宽、跟旁边文字隔多远都随字体飘（当年是拿一个空格去凑那段距离的）。图标的规矩：**尺寸写死在 `icons.tsx` 里**（14px，每颗在各自的地方都是这个数，调用点没有要调它的场景）、**颜色不给**（SVG 的 `stroke` 是 `currentColor`，跟文字一样由父元素的 `text-*` 定，悬停变色也归父元素）、`aria-hidden` 也不给（库里没拿到无障碍属性时默认加，三颗都是装饰；哪天某颗需要 `title`，它会自动变成不隐藏）。**别为了几个图标装整套图标 JS**：FA 那套要核心加 React 绑定，光核心 2026-09 实测 129 kB / gzip 30 kB——比当时整个应用还大；lucide 是一颗一个模块、包又是 `sideEffects: false`，import 三颗进产物只涨 3.3 kB（93.6 → 96.9 kB）。图标名只在 `icons.tsx` 出现一次：`IconButton` 自己带那颗叉，调用点连 `children` 都不用给。**分隔线、下划线这类「看着像符号」的东西用元素画，不要拿字符凑**（`Stepper` 的连接线是 `h-px w-4 bg-ctp-surface1`，最终页标题下那根是 `h-0.5 w-16 bg-ctp-mauve`——都不是 `—` 或 `|`）。2026-09 逐处查过一遍：界面里已经没有字体符号了，剩下带符号的字符（`→` / `≥` / `①②③` / `×`）全在注释里，`index.html` 的 `<title>` 里那个 `·` 是标签页标题、只能是文本，界面上唯一还出现的数字是清单分组右边那个**计数**（它是数据不是序号，且带 `tabular-nums`）
- **依赖按需装是常态，别自己造「精简版」**（2026-09 用户提醒：现代前端基本都按量构建）：Vite / Rollup 会把没 `import` 的摇掉，所以「装一个包」不等于「整个包进产物」——`lucide-preact` 有一千五百多颗图标，我们 import 三颗，产物只涨 3.3 kB（93.6 → 96.9，实测）。判断一个包值不值得装，看它的**形状**而不是大小：`sideEffects: false` + 一个功能一个模块（lucide 这样）就摇得干净；**摇不掉的是「核心本身是个运行时」那种包**（FA 的 `fontawesome-svg-core` 光核心 129 kB / gzip 30 kB，装三颗和装三十颗一样大，那才算「整套 JS」）。所以别因为怕大就去抄一份 `path` 数据、手写个精简版塞进仓库——那是把自己变成那个包的维护者
- **一个知识只有一个定义**：界面文案集中在 `src/lib/copy.ts`（只收两处以上用到的，值相同不等于同一条知识）；内容长什么样由 `src/lib/outro.ts` 的 `resolveOutro` 一处决定；框的外观是 `ui/inputs.tsx` 的 `BOX` / `BARE_INPUT` / `BareRow` / `BareTextArea`（裸多行框，三行的行数也写在里面）；排版与动效片段在 `ui/tokens.ts`；可点的整行是 `ui/LinkList.tsx`（页脚那两条链接用它）。**注释也照这条办**：像「窄屏贴边的面不给留白、里面的东西自己带一次」「名称与值上下排、对内紧于条间」这类跨文件成立的推论，留一处写全，别处只一句「跟别处同一条规矩」；一条原则管着的一对函数（`app.tsx` 的 `openForm` / `formAtLastStep`）就把那句想法挂在两者之间写一次，别各写一句
- 目录表叫 `_registry.ts(x)`：`src/surveys/` 是首页那些入口（全是数据，加一份 = 加一个文件 + 加一行）、`src/steps/` 是向导三步
- **保存图片＝把屏幕上那一份原样装进 SVG，再让浏览器画到方形画布上**（`src/lib/image.ts`）：**没有第二套渲染路径**。做法是把页面外壳（卡片与它继承来的那一套）整个克隆、**应用的 CSS 一并带上**、SVG 的视口宽度取 `window.innerWidth`——媒体查询、继承、百分比于是都在同一套条件下解，卡片排出来的东西（含每一处折行）就是屏幕上那一份；`data-card` 那一层量到的宽度还要**钉回克隆里**，因为图里没有滚动条，不钉它就会比屏幕上宽出十来像素、折行跟着变。那种「把每个元素算完的样式读出来、冻死在克隆上」的做法（`html-to-image` 那一类）会漂，正是因为它把一个文档里的计算结果搬到了条件不同的另一个文档里——2026-09 就是这么漂的（字号被压 0.1px、宽度差一个滚动条，成品与屏幕上的最后一行对不上），装过的那个库也一并撤掉了。方形是量出来的：边长取**卡片（含它那圈内边距）较长的那一条的两倍**，卡片按**原尺寸**摆在正中（渲染倍率写死 1，不跟设备的像素比走），四周于是各留半条最长边；底色取外壳的底色（不透明）。**控件不进卡片**：「返回编辑」与「保存为图片」在卡片外面那一行里（`data-chrome`，打印与存图都整块拿掉）——控件一进卡片，它占的位置就成了署名折行的一部分，页面上挪一下按钮、成品里的折行就跟着变（2026-09 栽过两次：一次是取景台里没收控件，一次是收了控件的位置）。字体全用系统的，不内嵌；文件名取标题，标题空着就叫「结尾页」
- 问卷是数据不是代码：题目写 `into` 决定答案落到结尾页哪里（四条去处写成一个字符串：`into: 'meta'` / `'block'` / `'title'` / `'footer'`；2026-09 从 `{ kind, label? }` 压扁的——可选的那层 `label` 11 道题一处都没写过，结尾页那行名字一律取题面），需要加工才写 `build`；每道题**先写 `into`**（数据里字段顺序也照它排——一份问卷是拿结尾页倒着写出来的）；加一份问卷不用碰组件
- **问卷答完是「以 `DEFAULT_CARD` 为底，问卷答到的部分盖上去」**（`buildFrom`），不是把内容清空：问卷没问到的去处保持默认值——没有页脚题的问卷答完，页脚那行署名还在（2026-09 修过：`buildCard` 当年从全空字面量起步，默认署名被写成了空串）。所以 `buildCard` / `Survey.build` 返回的是 `Partial<CardData>`：只有问到的去处才出现在结果里；问到却答空的标题 / 页脚写成空串（「这一块就是要空着」），元数据 / 文本块的空答案整条丢掉
- **回到表单只有一条原则：内容已经成型就停在最后一步，没有「刚才那一步」才从第一步进**（`app.tsx`）。`formAtLastStep`（`STEPS.length - 1`；2026-09 由 `backToForm` 改名——用它的地方不止「从结尾页退回来」了）现在给**两处**用：结尾页的「返回编辑」、**答完问卷**（2026-09 用户要求改的：问卷要问全，答完内容就成型了，再回第一步等于让人把刚答完的东西重看一遍；当年回第一步的理由是「问卷只问常见那几题，剩下的还得过一眼」，问卷问全之后这条不成立了，那段来历见本节），首页「编辑表单」走 `openForm`（**第一步**）——刚来的人没有「刚才那一步」，要顺读。将来加新入口先问一句：他**刚才看过内容没有**？看过就 `formAtLastStep`
- **按钮四种变体的差别只在颜色，几何一律一致**（`ui/buttons.tsx`）：外部高 40px、横向那圈 32px，**描边一律 1px**——有描边的（`ghost` / `danger`）用 `1px + px-[15px] py-[7px]` 把描边宽度从内边距里扣掉，没描边的（`primary` / `dangerSolid`）用 `px-4 py-2`。2026-09 用户提的：`danger` 原来写 `border-2`，一排按钮里就它一个粗边，看着像另一种东西（外框尺寸其实一样，纯粹是那根线的粗细）。所以**别给某个变体单独加粗描边、也别单独放大**；危险动作靠**颜色 + 文案**表达（「重置」点一下变实心红、字换成「确认重置」，逻辑不变），不靠加粗
- **页脚那种链接不是按钮，是它自己的一种东西**（`ui/LinkList.tsx`）：行为像整宽的按钮（点哪儿都行），长相却是链接（平时淡一档的次要文字，悬停才转 blue 加下划线），而且是竖向排列、行与行没有间隙的。三样凑在一起就是一份**导航清单**，语义照清单来——`<nav>` 里一个 `<ul>`，每项一个 `<li>` 一个 `<a>`；`<button>` 不行（干的是跳转不是动作，而且宽度会缩成文字那么宽）。行之间不写 margin / gap，上下内边距就是行自己的高度，所以两行紧挨着。2026-09 定的，别再退回 `<button>` 或加回悬停底色
- **同一个标题层级必须同款**：页面标题（`PageHeader` 的 h1）和页脚那两栏的标题共用 `ui/tokens.ts` 的 `HEADING`（`text-lg font-semibold`，正文色、字距照默认）；**印在卡片里**的标题是另一款——面板标题（`Panel` 的 h2）与清单的分组标题（`FilledList` 的 h3）带 `tracking-wide` 和淡一档的 `subtext1`，各只有一处所以写在组件里。页脚原本品牌是 `h2 + text-lg`、链接是 `h3 + text-base`，两栏并排却看着像一个管着另一个，2026-09 统一，并按用户要求提到跟页面标题同款（先试过压到卡片那一档，太轻）
- 注释写中文，**是设计想法的综述，不是实现或过程的记述**：一句想法说清这一块服务于什么目标、于是取了什么立场（要什么、不要什么）。它抽象的是**想法**——把「代码在做什么」或「事情怎么变成现在这样」换成抽象说法再重述一遍，仍旧不算；具体细节（数值、类名、改过几轮、哪天谁提的）更不进注释，那些的家是这份 `AGENTS.md`。底线：**点组件 / 文件 / 函数名可以**（谁渲染、谁决定，那是分工），**完整的类名与具体数值不行**（不只是风格问题：注释里的类名会被 Tailwind 当成真候选、编译进产物，见 `@source` 那条——**光秃秃的英文 utility 词也算**，`flex`、`grid`、`block`、`hidden`、`contents`、`container`、`transition` 写进注释一样会编译出规则，所以描述版面用中文说法：一维排布、二维表格、整列、贴边的面）；同一个文件里最多留**一处**「细节见 `AGENTS.md`」，别每层都重复。**prop 上的一行说明、以及「为什么写死、为什么不开成 prop」这类常量说明，也是想法与定义**，不属于细节——改注释时最容易把它们连根砍掉。**注释里唯一该留的「历史」是迁移锚点**：只用来认出旧档的那些字面量（旧存储键、旧模板声明、当年预填过的默认文案）留着，并说明它只用来认旧档、不代表今天——后人最容易把它当垃圾删掉，而删了没人报错。**例外是数据结构与类型的注释**：字段什么意思、取值范围、为空代表什么，就写在字段上——那不是细节，那本身就是定义（`survey/types.ts`、`config.ts`、每份问卷里的字段说明）；这个例外放宽的只是「细节禁令」，**不放宽「过程禁令」**——字段的来历（当年是什么、后来改成什么）照旧删
- 不写自定义 CSS：Tailwind v4 + catppuccin 的 `ctp-*` token 够用。两个例外都在 `style.css` 里：`.safe-area`（要读 `env(safe-area-inset-*)`，没有别的写法）和 `@custom-variant press`（Tailwind 没有现成的「悬停或按下」变体，而这正是全站可点件唯一要的那种反馈，见「动效」那一条）
- Tailwind 的扫描来源写死在 `style.css` 头上（`@import "tailwindcss" source(none)` + `@source "../src"` + `@source "../index.html"`）：默认它扫**整个仓库**，于是 `AGENTS.md` 里的中文散文（写着 `` `transition` ``、`` `rounded-none` `` 这种词）会被当成类名、真编译成规则——2026-09 实测产物里躺着 `.transition`、`.rounded-none`、`.contents`、`container` 这些没人用的死代码，删掉省了 700 来字节。以后加新目录（比如 `tools/`）要在这儿补一行 `@source`。**注释与字符串也算数**——扫描器只按纯文本提候选、不解析语法，所以 `src/` 里注释写下的类名一样会被编译：2026-09 `OutroPage.tsx` 的注释里（讲历史时）写了 `` `pt-12 pb-26` ``，产物里就真躺着一条 `.pb-26{padding-bottom:calc(var(--spacing) * 26)}`，52 字节的死代码。消掉它的办法不是躲着写，而是「注释只声明想法」那条——不写数字与类名，自然不会有这个漏子
- 入口是根目录的 `index.html`（Vite 的约定，它只是模板，产物是 `dist/index.html`）；里面的 `<title>` 是品牌名唯一一处没走 `COPY.brand` 的地方
- 路由用 hash（`#form`、`#outro`、`#<入口 id>`）：构建产物要能直接 `file://` 打开

## 响应式

- **只有三个模式，只看宽度**（三个数定在 `src/style.css` 的 `@theme`；判断交给 Tailwind 编译成 CSS，代码里不出现宽度数字，也没有 `matchMedia` / `ResizeObserver`）：窄 `< 30rem`（480px，`max-narrow:`）、中 `30rem–64rem`（不加变体的默认样子：页面留白 + 卡片 + 单栏）、大 `≥ 64rem`（`wide:`：分栏 + 右侧清单常驻）。窄那条线取 480px 是有讲究的：再往上页面留白与卡片都还舒展、元数据两栏也放得下，没必要提前把边距内化——**内化是给「一维的流」用的，不是给「有点窄」用的**；嫌不够紧也只动这一个数
- **比例那套（`landscape:` / `portrait:`）已经彻底退役**（2026-09 连最终页也换掉了，代码里再出现就是走回头路）；容器查询（`@container` / `@max-sm`）只在元数据那行试过一轮也收掉了——一个页面里并存两套判断迟早长歪。要加响应式行为，先问「它属于窄 / 中 / 大哪一档」
- 窄屏是**一维的流**（边距内化）：容器不再提供横向留白（`max-narrow:px-0`），面横向贴边并去掉侧边描边与圆角（`rounded-none border-x-0`），横向留白由文字 / 控件自己带一次 `px-inset`（`@theme` 的 `--spacing-inset`，全应用只有这 16px 一个数，取 16 是因为它跟正文原本的落点几乎一样、换过去观感不会突然变松，也是手机上通用的缩进）。裸控件在窄屏不带横向内边距（`BARE_INPUT` 的 `max-narrow:px-0`），否则框一道、控件一道叠成两道
- 推论：**贴边的面没法再给内容留边**，所以卡片里凡是裸文字 / 裸列表都得自己写 `max-narrow:px-inset`（`Field` 的标签、`FilledList` 的内容、首页卡片的说明、`GenerateStep` 的正文都是这么办的）
- 元数据行**窄屏一律上下排**——编辑态（`MetaEditor`）、清单（`FilledList`）、最终页（`OutroPage` 的 `MetaList`）三处同一条规矩，它算窄模式的一种样式变体，不按自己的容器宽度单独判。代价是窄屏更高（名称一行、值一行），这是这条规矩买来的，别为了「窄屏也放得下」偷偷改回两栏。上下排之后还有一条：**名称与值是一「条」，对内要明显紧于条间**——三处都是「对内 2px / 条间 8px」。清单直接给两个数（`max-narrow:gap-0.5` 与 `dl` 的 `max-narrow:gap-2`）；最终页那张表是 `grid gap-y-2` 一个数管两处，所以**每对 `dt`/`dd` 包一层**：中宽档那层写 `contents` 让它**整个消失**（`dt`/`dd` 必须仍是 `dl` 的直接子项，名称列的跨行对齐靠的就是这一点，所以不能用普通的 div 包），窄档它变成 flex 列、对内 2px，条间那 8px 还是 `gap-y-2` 给的。2026-09 用户提的（改之前 4px 对 6px，差 2px 被 26px 的行高整个吃掉，看着就是一坨），用户看过后说「这样看起来有节奏」
- **换行要么由档位定，要么由内容定，别让它悬着**：一行里放几样东西是**结构**，要按档位写死——页脚两端（向导那份内容多，`wide:flex-row` 才左右分列；最终页只剩署名一段，反过来写：默认一行，`max-narrow:flex-col` 才折过来靠左）、动作行、两栏；`flex-wrap` 只留给**条数 / 长度由数据决定**的排（`QuestionInput` 的选项、`Stepper` 的步骤条）。`GenerateStep` 的按钮行是唯一的兜底（两个短按钮，防 320px 以下挤爆），不算「内容驱动布局」的先例。最终页页脚原本是 `flex-wrap justify-between`，改回档位就是因为那条判断没写下来——看的人不知道它什么时候会换行
- 用 `grid` 就一定显式写列模板（`grid-cols-1`、`grid-cols-[minmax(0,1fr)_…]`）：不写的话那一列是隐式的 `auto`，按内容 max-content 算、**不会收缩**，输入框天生的固有宽度（400px 出头）会把整列顶出屏幕（2026-09 那次「485px 溢出」就是这么来的）
- 内禀宽度的另一头：**`<button>` 的宽度按内容收缩**，写 `display:block` 也撑不满父元素——2026-09 量到页脚「返回主页」那一行只有 64px（正好是文字宽，同一个位置的 `<a>` 是 367px）。要让**整行**可点就用 `<a>`，别给按钮打 `w-full` 补丁。反过来，**在 flex 列里它会被拉满整行**（`width: auto` 的项在交叉轴上默认拉伸；合成例子量到按钮 300/300）——列里要放一个内容宽的裸按钮就写 `self-start`；`AddButton` 那种本来就要整宽的自己带 `w-full`，不受影响，行里的按钮只管高度也不受影响
- **动效**只有渐变：`ui/tokens.ts` 的 `FADE` / `RISE` / `HOVER` 都长在同一份清单上（`opacity` / 文字色 / 底色 / 描边色 / `display`），别在组件里散着写 `transition-*`——一个元素只能有一份 `transition-property`，所以清单只在那个文件里写一次。基调 150ms / ease-out / 不回弹（目标是「别硬蹦」不是「炫」），`motion-reduce` 下完全不动；`flex-direction`、列数变化这类插不了值的只能用 `FADE` 淡一下遮住
- **几何量一概不插值**：内边距 / 描边宽度 / 圆角 / 位移 / 缩放都不在清单里——按下不缩放，跨窄屏线时零件不收放，形状变化直接跳。理由是插值出来的是「在动」，页面里一有东西在动，观者就得跟着重新找位置；2026-09 为此收掉两轮（选项按钮按下缩 2%、整页跨窄屏线一起收放），**别再加回来**。反馈一律靠颜色
- **跨档不做任何动画，连整体淡一下都不要**：试过用 `animation` 让整页在换档时淡进来（机制：媒体查询不会重播 `transition`，只有 `animation-name` 变化才重播，所以三个档得配三个名字），结果看起来不是「两个状态在换」而是「整页消失又重现」，比硬跳更差，已收掉。跨档想让人看得见，只能靠颜色或内容本身的变化
- **可点件的反馈写 `press:`，别直接写 `hover:`**（`src/style.css` 里那个自定义变体，展开成 `@media (hover: hover)` 里的 `:hover` **加**裸 `:active`）：悬停与按下**是同一个状态**，定义一次两边都有，也就不可能只加一半。2026-09 用户报的正是只加了一半的后果——「页脚的链接有悬停反应，触摸设备上点下去却什么都没发生」：那时全站 8 处可点件清一色只写了 `hover:`，而 Tailwind 的 `hover:` 编译进 `@media (hover: hover)`，触摸设备上那条规则根本不匹配，点下去是零反馈（headless Firefox 里 `(hover: hover)` 也是 false，探针量不到自己的悬停效果，同一个原因）。悬停那条**仍旧带媒体查询**，不然手机点完会粘在悬停色上；`:active` 写在后面，鼠标按住时两条都中、赢的是按下那条。产物是普通的 `:hover` / `:active`，兼容面跟原生一样。将来若想让按下比悬停更深一档，那才需要拆成两条，并把这个变体一起重新想
- 悬停 / 按下的反馈是**元素自己**变色，不位移也不换形状；贴在框里的图标按钮（×）连淡底都不给——浮出一块底色看着像框里又长出一个按钮。**链接清单**（`ui/LinkList.tsx`）也一样只变文字色：2026-09 给它加过悬停淡底（照参考站的 `.btn`），用户要求去掉——底色一浮出来就更像按钮，而它要的是「看着像链接」
- **最终页（`OutroPage`）也跟这三档**，只是它是「拿去截图的那一屏」，有三处自己的排版细节：① **要一致的是「栏」（一行）的宽，不是容器的宽**——栏宽 = 容器 − 2 × 内边距，所以容器上限按 **24rem 的栏宽**反推：窄中两档共用一个 `max-w-[26rem]`（416 = 一行 24rem + 左右各 16），宽档 `wide:max-w-[53rem]`（848 = 两栏各 24rem + `gap-12` 3rem + 左右各 16，两栏按 **1 : 1** 各分 384px），三档栏宽都是 **384px（24rem）**。**内边距只需要一个数**（`p-inset` = 16，就是全站那个）：居中那两档里容器比视口窄，一行到视口边的距离 = (视口 − 栏宽) / 2，跟在容器里留多少内边距**无关**；只有视口窄到把容器顶住（窄档）时，内边距才真成为那道边距——2026-09 用户指出这一点后，中宽档的 `p-12`（48）也并成 16，`max-narrow:` 在这个组件上彻底消失（窄中两档同款）。窄档的代价：视口在 416–480 之间时容器是居中的 26rem、两侧留白，**不是全宽**（这段原来写着「它本来就比 24rem 窄，于是自然全宽」，2026-09 读代码时改正）。来历：2026-09 用户先定「大屏保证两栏都有 24rem」（改前容器 `48rem`、一栏只有 312px），再报「中屏切到窄屏反而变宽」——当时三档容器同为 24rem，可中档内边距 48 把一行压到 288、窄档 16 反而有 352，**容器一样不等于栏宽一样**——再定「栏目的 max-width 三档一致」，最后并掉内边距；② 整块**横竖都居中**（`justify-center-safe`，内容比屏幕高时退回顶部排，不会被切掉上半截）；③ 三段（标题 / 主体 / 页脚）都在**同一个 flex 列**里，段间距一律 `gap-12`，页脚不单独挂在外层（它常驻，但没有谁的间距是挂在 margin 上的：哪一段不印，`gap` 自动少一份）；外层那圈内边距**四边同一个 `p-inset`**（16，全站那个数；并成 16 之后，内容高出视口、页面开始滚动时上下留白也从 48 变成 16——这是「内边距只留一个数」买来的）——2026-09 之前这里下面比上面多 56px、拿它当「重心上移 28px」的光学补偿，用户要求内边距取同一个值，那套补偿连同注释一起收掉了，别再悄悄加回来；它身上没有「面」（无卡片 / 底色 / 描边），所以窄屏的边距内化在它身上不用做任何事——内边距本来就是那个 16；`wide:` 才分两栏（左元数据 1 : 右文本块 1）；④ 页脚与元数据行都**只在最窄那一档折叠**（`max-narrow:`）——中档那个 24rem 一行放得下，没必要提前把结构拆开

## 内容与文案

- 类别名写全，不缩写（「逻辑红石音乐」不写成「逻辑红乐」）——标题、地址、首页卡片一律全称
- 选项表是常用值不是全集：长一点的单选都要留「自定义」的口子，自己写的那一句也要能进结尾页。**多行题（`kind: 'long'`）也能带 `options`**：预设是**一整段**现成的文字，所以竖着排成整宽的一块块（照单选那样横排会挤成小方块、还得左右扫读），点一条就整段填进多行框。两个形态跟单选**完全同一套**：互斥、点「自定义」才出框、框里拿预设那段字当底子改、× 退回预设、再点一下已选中的取消；点是**覆盖**不是追加（2026-09 用户定的，追加要先有「去掉其中一条」的语义，不值当）
- × 要**看起来**嵌在框里：靠组合拿到（`BareRow` 多传一个 `action`），不是塞进 `<input>` 里
- **没有兜底文案**（2026-09 收掉）：留空就是不印——标题空着，标题块（连下面那根横线）整个不渲染；页脚空着，署名那行不渲染（卡片外那一行控件还在，它们不跟内容一起消失）。**留空旁边那段距离也跟着走**：最终页「标题 → 主体」的 48px 由那两块的父层用 `flex gap` 给，不是挂在 `main` 上的 `mt-12`（2026-09 探针量到：无标题时「版面顶 → 内容顶」96px，明明该是 48）——给条件渲染的东西留间距就用 `gap`，别用会被孤立下来的边距。占位提示也不是数据：全站只有 `COPY.placeholder` 一句「不显示」，所有空框是同一个意思
- 预填值（`default`）是**真值**：框里一开始就写着，会一路印到结尾页；「默认不显示这一行」用**不写 `default`** 表达，而不是预填一句「不显示」。页脚那份初始值（`config.ts` 的 `DEFAULT_CARD.footerText`）也是真值，删掉就真没有。问卷数据里因此没有 `placeholder` / `rows` 这类只管控件长相的字段，多行框的行数是**写死的 3 行**（`ui/inputs.tsx` 的 `BareTextArea`，2026-09 从 5 行压下来的）：问卷的段落题与文本块的正文都走它，行数**不开成 prop**，调用点不管控件有多高
- 空答案整条丢掉：没填的题不在结尾页留一个空标签
- 清单三段（摘要 / 描述 / 页脚）**哪一段空着就在那一组里放一个虚线提示**（`EmptyHint` + `COPY.empty`「留空」）：空的是哪一段由它上面的小标题说明，提示只说「这一段是空的」，别写成一句解释。`EmptyHint` 只带**框内部**的内边距，所以清单的 `max-narrow:px-inset` 写在「有内容」的三个分支上而不是外层——两层叠着写，窄屏那个框会比别的行窄一圈
- 不用 `truncate` 之类截断内容：名称 / 值写长了就让它换行（`FilledList` 的标签列曾经是 `truncate`，会悄悄丢字）。清单是给人最后确认用的，宁可它高一点
- **文案不为某一档定制，也就不写方位、不暗示结构**：一句话得在这个页面的**每一档**都成立。向导页那句「按步骤填写内容，**右侧**实时确认已填信息」把宽档的分栏当成了全局事实——右栏只有宽档有，中窄档连「有清单」都只发生在某几步；「卡片**右上角**的保存按钮」这种话一写就错——中窄档根本没有「右上角」那回事。要指位置就只用**不随档位变**的粗说法（「下方按钮」「那一行里」），拿不准就别提；同理不写「第 N 步」「点右边那个」，步骤数、档位结构都会变。文案里出现方位词，先问它在窄档还成不成立

## 环境

- 技术栈：Vite + Preact + TypeScript（strict）+ Tailwind v4 + `vite-plugin-singlefile`；图标用 `lucide-preact`（ISC，按需）。**存图不装库**：2026-09 试过一轮 `html-to-image`（产物 99.5 → 114.4 kB），它靠「克隆 + 把算完的样式冻死」重建页面，跟屏幕上的排版会漂，已撤掉（产物回到 101.7 kB，`src/lib/image.ts` 自己几十行）
- 仓库：`omninbs/outro`，公开；线上 https://omninbs.github.io/outro/
- 数据存在浏览器 localStorage（`outro.card.v2`；旧键 `colophon.card.*` 会被迁移过来），不经过任何后端
- 发布：`.github/workflows/deploy.yml`，push 到 main 自动跑（node 22 + configure-pages@v6 / upload-pages-artifact@v5 / deploy-pages@v5）。注意 `concurrency: pages` 且 `cancel-in-progress: false`——**卡住的 run 会挡住后面的**，得去 Actions 页面把它取消
- 要发一份给别人：`cp dist/index.html dist/outro.html`，自包含、`file://` 直接打开，对方的数据只存在他自己浏览器里
- `index.html` 的 `viewport-fit=cover` 与 `style.css` 的 `.safe-area` 是**一对**：`.safe-area` 只挂在最外层根节点（`PageShell`）上，内部的留白照旧由各层间距给。删掉那个 `meta`，这段安全区 padding 会**静默失效**，没有任何闸门会报
