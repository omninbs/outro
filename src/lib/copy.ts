/**
 * 界面文案：一份「命名表」，一处定义、各处引用。
 *
 * 同一句话写在两个地方，就是两个会各自漂移的定义——步骤名既在步骤条上又在清单分组里，
 * 字段名既在输入框标签上又在清单那一行上，输入框的占位提示同时还是最终页留空时的兜底。
 * 这些都是同一条知识，只能有一个定义处。
 *
 * 边界：只收「名字」与「兜底值」。整句的说明（页脚标语、空列表提示、认不出问卷时的提示）
 * 只有一个地方用得到，留在原处更好读——搬进来只是把分散换成绕路。
 * 值相同也不等于同一条知识：`fallback.title` 是最终页印出来的兜底文字，
 * `field.title` 是输入框的字段名，两者碰巧都写成「标题」，将来会各走各的，所以各留一个键。
 */
export const COPY = {
	/** 应用名：首页、向导页、页脚三处的标题（index.html 的 <title> 是第四处，改名前记得它） */
	brand: '结尾页生成器',

	/** 三个步骤：步骤条、该步的面板标题、清单里的分组都取这里 */
	step: {
		summary: '摘要',
		describe: '描述',
		generate: '生成',
	},

	/** 区块名：步骤里的面板标题与清单里的分组标题 */
	section: {
		title: '页面标题',
		meta: '元数据',
		blocks: '文本块',
		footer: '页脚',
		list: '清单',
	},

	/** 字段名：输入框的标签，以及清单里对应那一行的标签 */
	field: {
		title: '标题',
		footerText: '页脚文字',
		metaLabel: '名称',
		metaValue: '填写内容',
		blockLabel: '小标题（可留空）',
		blockText: '正文',
	},

	/** 动作名：按钮上的字 */
	action: {
		addMeta: '添加元数据',
		addBlock: '添加文本块',
		remove: '删除',
		generate: '生成',
		reset: '重置',
		confirmReset: '确认重置',
		backHome: '返回主页',
	},

	/** 占位提示，同时也是最终页留空时的兜底：输入框里的灰字和印出来的字是同一句 */
	fallback: {
		title: '标题',
		footer: '由 omninbs/outro 生成',
	},
} as const;
